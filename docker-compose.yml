services:
  discord-bot:
    build: .
    container_name: ctfpilot-discord-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./challenge_issues.json:/app/challenge_issues.json
      